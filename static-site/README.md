# Static site starter

A plain `index.html` deployed to Orbit Cloud's static hosting — no build step.
For framework SPAs (Vite, Astro, Next static export...), set your build
command and output directory in the service settings; common output dirs
(`dist`, `build`, `out`, `public`) are auto-detected.
